{
  "compilerOptions": {
    "module": "NodeNext", // Specifies the output module to be used
    "moduleResolution": "NodeNext",
    "target": "ES2022", // Target output for code
    "outDir": "dist", // Output path for build directory
    "esModuleInterop": true,
    "strict": true /* Enable all strict type-checking options. */,
    "skipLibCheck": true /* Skip type checking all .d.ts files. */,
    "baseUrl": ".", // Base path for imports
    "paths": {
      "@/*": ["src/*"] // Relative path for imports (alias)
    }
  },
  "files": ["src/type.d.ts"], // Files used to define global types for the project
  "include": ["src/**/*"] // Include path for files to build
}
